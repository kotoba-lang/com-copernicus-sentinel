{:paths ["src" "test"]
 :tasks {test {:doc "Run the cljc contract + behavioral test under babashka"
               :extra-paths ["test"]
               :requires ([copernicus_sentinel.main-test])
               :task (let [{:keys [fail error]} (clojure.test/run-tests 'copernicus_sentinel.main-test)]
                       (System/exit (if (pos? (+ fail error)) 1 0)))}}}
